import { ColorsFilterPipe } from './colors-filter.pipe';

describe('ColorsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
